<div class="b-container @(FillHeight ? "fill-height" : "")" style="@Style">@ChildContent</div>

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [Parameter]
    public string Style { get; set; } = "";
    [Parameter]
    public bool FillHeight { get; set; }
}